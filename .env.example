# =============================================================================
# ExpenseSyncBot - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=false
LOG_LEVEL=INFO

# CORS Configuration
# Comma-separated list of allowed origins (use ["*"] for all origins)
# Example for production: CORS_ORIGINS=["https://app.example.com","https://n8n.example.com"]
CORS_ORIGINS=["*"]

# -----------------------------------------------------------------------------
# MCP Server Configuration (C# Server via SSE)
# -----------------------------------------------------------------------------
# URL of the C# MCP server SSE endpoint
MCP__SERVER_URL=http://localhost:5000/sse
MCP__CONNECTION_TIMEOUT=30.0
MCP__RETRY_ATTEMPTS=3
MCP__RETRY_DELAY=1.0

# -----------------------------------------------------------------------------
# Orchestrator Configuration
# -----------------------------------------------------------------------------
# LLM provider for the main orchestrator (openai, openai-gpt4, gemini, deepseek, groq, groq-fast)
ORCHESTRATOR__LLM_PROVIDER=openai
# LLM provider for categorization (GPT recommended for extraction)
ORCHESTRATOR__CATEGORIZER_PROVIDER=openai
# LLM provider for validation (Gemini recommended for business rule validation)
ORCHESTRATOR__VALIDATOR_PROVIDER=gemini
# Maximum categorization retry attempts on validation failure
ORCHESTRATOR__MAX_CORRECTION_ATTEMPTS=3

# -----------------------------------------------------------------------------
# LLM API Keys
# -----------------------------------------------------------------------------
# OpenAI API Key (required for openai/openai-gpt4 providers)
# Get API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your_openai_api_key_here

# Google AI API Key (required for gemini provider)
# Get API key from: https://aistudio.google.com/apikey
GOOGLE_API_KEY=your-google-api-key

# DeepSeek API Key (required for deepseek provider)
# Get API key from: https://platform.deepseek.com/
DEEPSEEK_API_KEY=your-deepseek-api-key

# Groq API Key (required for groq/groq-fast providers)
# Get API key from: https://console.groq.com/keys
GROQ_API_KEY=your-groq-api-key